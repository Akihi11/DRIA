# env.docker.example
# DRIA Docker部署环境变量配置示例
# 复制此文件为 .env.docker 并填入实际配置

# ====================
# 主项目PostgreSQL配置
# ====================
# 方式1: 使用完整的连接字符串（推荐）
# 注意：如果主项目一致（clone自同一仓库），使用以下默认值
DATABASE_URL=postgresql+psycopg2://admin:password@poc-pg:5432/unified_db
# password修改为admin123

# 方式2: 使用分离的配置项（如果上面的方式不工作）
# 如果主项目的数据库配置不同，请修改以下值
POSTGRES_HOST=poc-pg
POSTGRES_PORT=5432
POSTGRES_DB=unified_db
POSTGRES_USER=admin
POSTGRES_PASSWORD=password
# password修改为admin123
# ====================
# 主项目Ollama配置
# ====================
# Ollama服务地址（使用主项目的服务名称）
# 注意：如果主项目一致，服务名称为 poc-ollama
OLLAMA_URL=http://poc-ollama:11434
# 或者如果Ollama在主机上运行：
# OLLAMA_URL=http://host.docker.internal:11434

# Ollama模型名称（根据实际使用的模型修改）
OLLAMA_MODEL=qwen2.5:3b-instruct

# ====================
# 应用配置
# ====================
DEBUG=False
LOG_LEVEL=INFO
DEFAULT_LLM_PROVIDER=local

# ====================
# 其他LLM配置（可选）
# ====================
# 如果需要使用其他LLM提供商，取消注释并填写
# DEEPSEEK_API_KEY=your_deepseek_api_key
# DEEPSEEK_BASE_URL=https://api.deepseek.com
# DEEPSEEK_MODEL=deepseek-chat

# KIMI_API_KEY=your_kimi_api_key
# KIMI_BASE_URL=https://api.moonshot.cn
# KIMI_MODEL=moonshot-v1-8k

# OPENAI_API_KEY=your_openai_api_key
# OPENAI_BASE_URL=https://api.openai.com
# OPENAI_MODEL=gpt-4

# ====================
# 文件上传配置
# ====================
MAX_FILE_SIZE=104857600
ALLOWED_EXTENSIONS=.csv,.xlsx,.xls

