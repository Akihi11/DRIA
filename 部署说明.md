# DRIA 系统部署说明

## 📁 实际项目结构

```
DRIA/
├── backend/            # 后端（FastAPI）
│   ├── api/
│   │   ├── main.py     # FastAPI 应用（含路由注册）
│   │   └── routes/     # 具体业务路由
│   ├── models/         # Pydantic 数据模型
│   ├── services/       # 业务逻辑与报表生成
│   ├── reports/        # 生成的报表输出
│   ├── uploads/        # 上传的源数据
│   ├── config.py       # 配置与 settings
│   ├── requirements.txt
│   ├── main.py         # 后端启动入口（uvicorn.run）
│   └── start.bat       # Windows 一键启动脚本
├── frontend/           # 前端（React + Vite）
│   ├── src/
│   ├── vite.config.ts  # Vite 代理到 http://127.0.0.1:8000/api
│   ├── package.json
│   └── start-frontend.bat
├── docs/               # 文档
├── 环境配置指南.md
└── 部署说明.md
```

## 🔧 环境要求

- Python 3.12+
- Node.js 18+（Vite 5 要求）
- Windows 10/11（本文以 Windows 命令为例）

建议在项目根目录 `DRIA/` 下创建 `.env`，参考《环境配置指南.md》。

## 🆕 第一次部署（全新环境）

1. 配置 Python 与 Node 环境

   - 确认 `python --version` 为 3.12+，`node -v` 为 18+
   - Windows 建议通过 Microsoft Store 或官方安装包安装

2. 初始化后端依赖与虚拟环境（端口 8000）

   ```cmd
   cd backend
   python -m venv venv
   venv\Scripts\activate
   python -m pip install -r requirements.txt
   ```

3. 初始化前端依赖（端口 5173）

   ```cmd
   cd ..\frontend
   npm install
   ```

4. 首次运行验证
   - 启动后端：
     ```cmd
     cd ..\backend
     python main.py
     ```
   - 观察控制台：
     ```
     INFO:     Uvicorn running on http://127.0.0.1:8000
     INFO:     Application startup complete.
     ```
   - 启动前端：
     ```cmd
     cd ..\frontend
     npm run dev
     ```
   - 浏览器访问：
     - 前端：http://localhost:5173
     - API 文档：http://127.0.0.1:8000/api/docs
     - 健康检查：http://127.0.0.1:8000/api/health

Vite 代理已在 `frontend/vite.config.ts` 配置，将 `/api` 代理到 `http://127.0.0.1:8000/api`。

## ▶ 日常使用（已完成初始化后）

- 启动后端（推荐脚本）：

  ```cmd
  cd backend
  start.bat
  ```

  或手动：

  ```cmd
  cd backend
  venv\Scripts\activate
  python main.py
  ```

- 启动前端：

  ```cmd
  cd frontend
  npm run dev
  ```

- 常用地址：
  - 前端：http://localhost:5173
  - API 文档：http://127.0.0.1:8000/api/docs
  - 健康检查：http://127.0.0.1:8000/api/health

## 🌐 常见访问入口

- 前端应用: http://localhost:5173
- 后端 API 文档: http://127.0.0.1:8000/api/docs
- 后端健康检查: http://127.0.0.1:8000/api/health

健康检查预期返回（示例）：

```json
{
  "status": "healthy",
  "timestamp": "2025-01-01T12:00:00",
  "version": "1.0.0"
}
```

## ✅ 快速功能验证

1. 在前端页面上传 CSV/Excel 文件，观察上传进度与返回的文件信息
2. 在对话页输入需求（如“生成稳定状态报表”），收到配置建议
3. 按提示生成报表，至 `backend/reports/` 查看是否生成并可下载

## ❗ 常见问题与排查

- 端口占用（8000 或 5173）：
  ```cmd
  netstat -ano | findstr ":8000"
  taskkill /PID <PID> /F
  ```
- 无法访问接口：先确保后端已启动并监听 8000，前端通过 5173 代理到 `/api`。
- 依赖问题：优先使用 `python -m pip install -r requirements.txt`，避免混用多个 Python。

---

完成以上步骤后，即可在浏览器通过 `http://localhost:5173` 使用系统，后端接口统一在 `http://127.0.0.1:8000/api` 提供服务。
